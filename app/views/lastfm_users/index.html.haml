%h1 Last.fm users

%ul#users
  - @lastfm_users.each do |lastfm_user|
    %li{:class=>cycle(:even,:odd)}
      .username= link_to h(lastfm_user.username.capitalize), lastfm_user_path(lastfm_user)
      .logo=link_to image_tag('github.png', :height=>16), "http://github.com/#{lastfm_user.username}"
      .logo=link_to image_tag('last_fm.png', :height=>16), "http://www.last.fm/user/#{lastfm_user.username}"
      
.clear_both
= will_paginate @lastfm_users
